// Este arquivo é um exemplo de configuração para produção no Render.
// Substitua a URL abaixo pela URL pública do backend gerada pelo Render após o deploy do backend.

module.exports = {
    API_URL: 'https://upultimate-6.onrender.com', // Altere para a URL real do backend
    APP_PATH: '/usr/src/app',
    CONFIG_PATH: '/usr/src/app/config'
}
